name: Check Herdr manifests

on:
  workflow_call:

jobs:
  check-herdr-manifests:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - name: Check Herdr manifest drift
        run: python3 scripts/check-herdr-manifests.py
